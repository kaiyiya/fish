/**
 * 根据 _long_wt.json 生成长文本替换表（按 from 长度降序写入 replacements-wave2.js）
 */
const fs = require('fs');
const path = require('path');

const long = JSON.parse(
  fs.readFileSync(path.join(__dirname, '_long_wt.json'), 'utf8'),
);

const toByIndex = {
  // 0 NestJS+TypeORM+Redis+Docker+Nginx 大段
  0: `本项目后端采用 NestJS 10.x：用模块划分业务域，controller 承接 HTTP，service 写规则，guard/pipe 做鉴权与入参校验。数据访问走 TypeORM，目标库为 MySQL 8.0；JWT 负责登录态。文中提到的 Redis、Docker、Nginx 属于可插拔部署能力：若未在实测环境启用，则不把其性能写死为已验证结论。`,
  // 1 CNN/MobileNet/SE/ImageNet 大段
  1: `卷积网络用局部感受野堆叠层次特征，池化再压空间尺寸，末端全连接完成品种分类。ImageNet 预训练提供了可迁移的纹理与形状先验；在本课题的鱼图数据上微调即可收敛。MobileNet 用深度可分离卷积降低乘加量，SE 分支对通道做自适应加权。训练阶段配合随机几何与颜色扰动减轻过拟合；离线评估沿用 Accuracy、Precision、Recall、F1 与 Top-K 等常用口径。`,
  // 2 Taro（保留 XML 中的 &quot;）
  2: `Taro 允许用 React/TS 维护一套页面逻辑，再编译到微信与 H5 等端；其宣传语 &quot;一次编写，多端运行&quot; 在本仓库里体现为商品流、识图与订单等页面的复用。状态侧选用 Zustand，与 Hooks 搭配即可；平台差异由 Taro 运行时与条件编译吸收。微信端最终仍落为小程序原生组件与生命周期，只是对业务代码做了封装。`,
  // 3 混合推荐+冷启动+缓存
  3: `混合推荐把 UserCF、ItemCF、内容与热度等信号做加权或分场景切换：新客偏热度与内容，老客偏协同过滤。Precision/Recall/F1、覆盖率与多样性用于离线对比；冷启动时以类目、标签或热门兜底。工程上若加缓存，可按「用户 + 策略」设 TTL；增量更新与并行化则视部署规模再启用。`,
  // 4 英文摘要第二段 — 已优化，保持不动
  4: null,
  // 5 ItemCF/内容特征 段（以句号开头）
  5: `。ItemCF 侧先算物品共现与相似度，再把用户历史点击映射到相近 SKU；度量仍可取余弦或皮尔逊。内容侧则把价带、类目、标签与简介关键词拼成向量，用余弦对齐用户偏好。两类特征可并行产出候选，再在排序层合并。`,
  // 6 推荐算法模块流程
  6: `推荐域在本项目中拆成「矩阵构建 → 相似度/特征 → 打分 → 截断」。UserCF 走共现用户；ItemCF 走共现商品；内容分支用标签与价带；热度按浏览/成交加权。混合策略按行为稀疏度切换：行为少给热度与内容，行为稳定再加重协同过滤。`,
  // 7 开发环境与目录
  7: `后端源码以 src 为根：modules 下按域划分子目录，常见分层为 controller / service / entity / dto。controller 只处理协议与校验，service 承载用例，entity 对齐表结构，dto 描述入参与返回。测试与配置与业务目录平级，便于 CI 与本地一键启动。`,
  // 8 后续工作（含 Wide &amp; Deep）
  8: `后续可在三方面继续迭代：其一，补样本与难例挖掘，抬高长尾鱼种识别；其二，在排序层显式加入多样性或新颖性约束，缓解列表同质化；其三，按访问量评估读写分离、分库分表与推理批处理。深度排序侧可尝试 NCF、Wide &amp; Deep、DeepFM 等模型，但需以真实日志与离线回放验证再上线。`,
  // 9 模型训练流程
  9: `离线训练链路为：采集与清洗鱼图 → 划分训练/验证/测试 → 载入 MobileNetV3 与 ImageNet 权重 → 调整最后一层类别数。优化器选 Adam，配合早停与验证集监控；指标含准确率、宏平均 Precision/Recall、F1。产物可导出为 H5 或 SavedModel，再交给部署脚本。`,
  // 10 推荐性能优化
  10: `推荐链路的工程优化集中在三类：为用户-物品行为表建合适复合索引；在确有缓存时再用键值缩短重复计算；增量与并行只在数据量上来后启用，避免过早复杂化。未部署 Redis 时，以数据库与接口实测为准。`,
  // 11 用户认证
  11: `认证子系统覆盖注册、登录与 JWT 校验：注册阶段校验格式、查重并 bcrypt 存密；登录成功后签发短期 Token；网关或守卫解析 Token，把 userId 写入上下文。登出可通过黑名单或缩短有效期实现，视部署而定。`,
  // 12 用户接口说明
  12: `用户相关 HTTP 入口包括注册、登录、资料读与资料改；除注册/登录外，其余接口在 Header 携带 Bearer Token。返回体与错误码与前端约定一致，便于小程序统一拦截。`,
  // 13 致谢（语气略作调整，避免套话堆叠）
  13: `写到致谢这一页时，本科阶段的学习与毕设推进也一并收束。感谢导师在选题与修改上的把关，也感谢同学在联调与排版上的提醒。四年里课堂与项目交织，这段经历本身比任何套话都更真实。`,
  // 14 推荐系统+UserCF 开头（保留公式引导句）
  14: `推荐系统把隐式反馈转成可检索的相似结构：要么找相近用户，要么找相近商品。UserCF 先在共现矩阵上估计用户相似度，再把邻居喜欢的、目标尚未交互的物品推前。相似度常用余弦或皮尔逊。余弦相似度计算公式为：`,
  // 15 模型选择
  15: `基座网络选用 MobileNetV3-Large，并以 ImageNet 权重初始化；全连接头替换为鱼种类别数。训练策略上可冻结前几层仅训头部，也可端到端微调——数据量有限时前者更稳，样本增多后再放开全量参数并调低学习率。`,
  // 16 模型部署
  16: `推理侧可将训练产物转为 TensorFlow.js 便于端侧试验，或导出 ONNX 以对接不同运行时；线上更常见的是把 SavedModel 封进容器，与 API 网关同域部署。请求路径为：预处理 → 模型前向 → 置信度阈值与 Top-K 返回。`,
  // 17 前端结构
  17: `小程序端以 src/pages 组织路由页面，components 放复用块，utils 封装请求与存储，store 放 Zustand 切片。样式与脚本分文件维护，类型定义尽量覆盖接口模型，减少联调歧义。`,
  // 18 文献综述 LeCun/AlexNet 段
  18: `图像识别主线从卷积与池化起步：LeCun 等早期工作证明局部权值共享可行；AlexNet 在 ImageNet 上拉开深度网络序幕，此后 VGG、ResNet、Inception 等结构继续抬升精度。与本课题直接相关的是轻量骨干与可部署性，因此后文选用 MobileNet 系列。`,
  // 19 模型构建 TensorFlow
  19: `实现上可用 TensorFlow 或 PyTorch 搭建同名骨干，加载预训练权重后替换分类头；优化器、批量与轮次按验证集早停调整。保存时同时导出评估曲线与混淆矩阵，便于撰写实验章节。`,
  // 20 实践价值
  20: `在应用侧，拍照识鱼能把 SKU 对齐到真实品种，减少“搜不到”的挫败；推荐则把长尾商品暴露给合适人群。同类技术也可迁移到其他生鲜类目，只需替换标注与商品库。`,
  // 21 订单模块
  21: `订单子域负责下单、分页查询与状态机推进：创建时锁库存与校验价格；查询走索引与时间范围；状态变更触发发货或退款钩子。异常路径如库存不足、价格变动需返回可理解错误码。`,
  // 22 热门推荐公式段
  22: `热度榜可把浏览、成交、收藏按权重合成 popularity(i)=α·views+β·purchases+γ·collections，系数需随运营策略调参；再按分排序截断即可作为冷启动兜底。`,
  // 23 英文摘要段一 — 已优化
  23: null,
  // 24 统计模块
  24: `统计页汇总曝光、点击、转化与推荐命中等埋点，可按用户、商品与时间切片；用于观察策略切换是否合理，而不是替代离线 A/B。`,
  // 25 学校声明页 — 保持原文，避免与校方固定表述不一致
  25: null,
  // 26 数据预处理
  26: `输入端统一缩放到 224×224，保留纵横比时用信箱填充；增强包含随机翻转、轻微旋转与颜色抖动。像素按 ImageNet 均值方差归一化，以减少与预训练分布的偏移。`,
  // 27 模型部署 Docker（修正原文双句号）
  27: `推理服务可随 Docker Compose 与后端、数据库一并编排；镜像内打包模型、依赖与启动脚本，减少“本机可跑、服务器不行”。多进程/多线程用于并发推理时，要注意批大小与 CPU/GPU 配额。`,
  // 28 数据库性能测试
  28: `在样例数据量下，单表与简单连接查询保持毫秒级；为外键与筛选列补索引后，计划从全表扫变为索引Seek，延迟会明显下降。具体数值随数据规模变化，应以当时 explain 与压测为准。`,
};

const pairs = [];
for (let i = 0; i < long.length; i++) {
  const to = toByIndex[i];
  if (to == null) continue;
  pairs.push([long[i], to]);
}
pairs.sort((a, b) => b[0].length - a[0].length);

const out =
  '// 由 build-replacements-wave2.js 生成；勿手改 from 串\n' +
  'module.exports = ' +
  JSON.stringify(pairs, null, 2) +
  ';\n';
fs.writeFileSync(path.join(__dirname, 'replacements-wave2.js'), out, 'utf8');
console.log('written', pairs.length, 'pairs');
